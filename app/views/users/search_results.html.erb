<h2 class="container text-center">「<%= params[:q][:name_cont] if params[:q].present? %>」の検索結果</h2>
<div class="container text-center ">
  <% if @user_name.present? %>
    <div class=" text-center col-12 ">
      <% @user_name.each do |result| %>
        <%= result.name %><br>
          <%= link_to "フォロー外す", user_relationships_path(@user.id), method: :delete,class: "box2-heart" %><br>
          <%= link_to "フォローする", user_relationships_path(@user.id), method: :post,class: "box2-heart-fill" %><br>
      <% end %>
    </div>
  <% else %>
    <h3>一致する結果がありませんでした。</h3>
  <% end %>
</div>